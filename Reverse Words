class Solution 
{
    String reverseWords(String S)
    {
        String a[]=S.split("\\.");
        String res="";
        for(int i=a.length-1;i>=0;i--){
            res+=a[i];
            if(i!=0)
            res+=".";
        }
        return res;
    }
}
